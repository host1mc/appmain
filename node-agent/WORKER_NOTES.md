# Worker notes

- 2026-08-21 — three additive error-message/handler fixes (code only, behavior preserved):
  - `node_agent/storage.py:151` (`write_bytes`) — `raise ValueError("uploaded file is larger than 150 MB")` -> `raise ValueError(f"uploaded file is larger than {MAX_UPLOAD_FILE_BYTES // (1024 * 1024)} MB")`. Intent: derive the megabyte figure from the constant so the message can't lie if `MAX_UPLOAD_FILE_BYTES` changes (matches how `_check_quota` interpolates `STORAGE_MB`).
  - `node_agent/storage.py:128` (`read_text`) and `node_agent/storage.py:139` (`write_text`) — `raise ValueError("file is too large for the browser editor")` -> `raise ValueError(f"file is too large for the browser editor (max {MAX_TEXT_FILE_BYTES // (1024 * 1024)} MB)")`. Intent: state the size limit like every other size message in the file; identical new message at both call sites.
  - `node_agent/app.py:242` (new `@app.errorhandler(404)` `route_not_found`) and `node_agent/app.py:249` (new `@app.errorhandler(405)` `method_not_allowed`), registered before the catch-all `internal_error` handler -> each returns `jsonify(ok=False, error=...), <code>`. Intent: routing 404/405 (werkzeug NotFound/MethodNotAllowed) previously fell through `internal_error`'s `isinstance(exc, HTTPException): return exc` branch and returned Werkzeug's HTML body; now they return the JSON envelope like every other error (mirrors the existing 413 handler). Numeric status codes used, so no import change; `internal_error` catch-all left unchanged as the final fallback.
